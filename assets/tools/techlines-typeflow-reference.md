# 技术线稿与图文混排 · 视频迭代（2026-09-08）

对应关系：
- `9e38458dd6b8ef5362b1f04ce0e533a6.mp4` → `techlines`，参考 0–18 秒的九宫格，以及约 20–24 秒的密集线稿成品。
- `b61c49bfdaa4274e8c79a49f3d9e68f3.mp4` → `typeflow`，参考轻字混排、彩色半调、像素拼贴、三层细轮廓和隐藏主体后的空心轮廓。

沿用原工具 ID、入口和导出方式。未新增重复工具。视频仅作为视觉参考，未把视频帧作为默认素材；内置素材由本地绘图代码生成。所有默认成品均无工作室或平台水印。

技术线稿：支持独立的 2–6 行/列、逐格居中或自由散布、九个不同的有机轮廓、彩色错位叠印、网格开关和主体数量/尺寸。保留原先三套参数预设，新增两套视频预设。

图文混排：逐行读取主体轮廓以计算文字可用区间；空心、网点也保留完整主体占位。长英文按可用宽度等比缩小，不直接丢弃。未排入的文案在画布外提示。四主体示例、主体数量/尺寸、直接拖动、原图开关和五套视觉预设可组合使用。切换预设保留自定义文案。

限制：继续使用本地环境色分离，不是语义分割模型。复杂背景图片可能需要先抠图或调整阈值。内置素材是自绘示例，不等同于视频中的摄影素材。字号、主体数量过大时可能排不完全部文字，需根据提示调整。

验证：Node 执行 package.json 中 npm test 对应命令（当前环境未提供 npm）：`node --test server/*.test.js`，30 项通过。浏览器检查灵感库、风格体系、模板工坊和工具箱，无控制台错误；验证五套混排预设、拖动重排、SVG 与两种 PNG 导出，并打开导出图进行视觉检查。源码本地更新，未执行线上部署。

2026-09-09 收尾：工具箱卡片使用本工具实际导出的无水印预览图，分别保存为 `techlines-preview.png` 和 `typeflow-preview.png`。
